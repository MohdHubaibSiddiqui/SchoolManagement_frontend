import {useEffect,useState} from 'react'; import {getClasses} from '../../services/api'; import {School} from 'lucide-react';
export default function Classes(){const [classes,setClasses]=useState([]);useEffect(()=>{getClasses().then(r=>setClasses(r.data.data||r.data||[]))},[]);return <><div className="page-head"><div><span className="kicker">ACADEMICS</span><h2>Classes</h2><p>School classes configured from Class 1 to Class 10.</p></div></div><div className="class-admin-grid">{classes.map(c=><div key={c._id} className="admin-class-card"><div className="class-number">{c.classNumber}</div><div><b>{c.name}</b><span>Monthly fee: ₹500 Day Scholar · ₹300 Hostler</span></div><School size={22}/></div>)}</div></>}
