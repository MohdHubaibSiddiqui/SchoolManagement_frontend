import {useEffect,useState} from 'react'; import {Plus,Pencil,Trash2,X} from 'lucide-react'; import {getFinanceUsers,createFinanceUser,updateFinanceUser,deleteFinanceUser} from '../../services/api';
export default function FinanceUsers(){const [users,setUsers]=useState([]),[modal,setModal]=useState(null),[form,setForm]=useState({name:'',email:'',password:'',phone:''});const load=()=>getFinanceUsers().then(r=>setUsers(r.data.data||r.data||[]));useEffect(load,[]);const open=u=>{setModal(u?'edit':'add');setForm(u?{...u,password:'',_id:u._id}:{name:'',email:'',password:'',phone:''})};const save=async e=>{e.preventDefault();if(modal==='edit')await updateFinanceUser(form._id,form);else await createFinanceUser(form);setModal(null);load()};const del=async id=>{if(confirm('Delete this finance user?')){await deleteFinanceUser(id);load()}};return <><div className="page-head"><div><span className="kicker">ACCESS CONTROL</span><h2>Finance Users</h2><p>Manage users who can record fee payments.</p></div><button className="primary-btn" onClick={()=>open()}><Plus size={18}/> Add Finance User</button></div><div className="table-card"><table><thead><tr><th>Name</th><th>Email</th><th>Phone</th><th>Status</th><th>Action</th></tr></thead><tbody>{users.map(u=><tr key={u._id}><td><b>{u.name}</b></td><td>{u.email}</td><td>{u.phone||'—'}</td><td><span className="badge">{u.status}</span></td><td><button className="icon-btn" onClick={()=>open(u)}><Pencil size={16}/></button><button className="icon-btn danger" onClick={()=>del(u._id)}><Trash2 size={16}/></button></td></tr>)}</tbody></table></div>{modal&&<div className="modal-backdrop"><div className="modal"><button className="close" onClick={()=>setModal(null)}><X/></button><h3>{modal==='edit'?'Edit finance user':'Add finance user'}</h3><form onSubmit={save} className="form-grid"><label>Name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Email<input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Phone<input value={form.phone||''} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Password<input type="password" required={modal==='add'} placeholder={modal==='edit'?'Leave blank to keep current':''} value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><button className="primary-btn full">Save User</button></form></div></div>}</>}
